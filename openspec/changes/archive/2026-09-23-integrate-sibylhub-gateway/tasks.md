# Tasks

## 1. Evidence, inventory, and approvals

- [x] 1.1 Inventory gateway-owned and third-party identifiers across source, manifests, configuration, scripts, docs, CI, and assets; classify each as first-party, gateway-owned external identifier, preserved third-party/protocol/upstream identity, control-plane integration, or provenance/license, and record the result in a reviewable migration matrix with affected files and evidence commands. Verify that every `aisix`, `AISIX`, API7, and `moonming` occurrence has a classification and that no protocol, provider, or upstream identifier is marked for blind replacement. _(Matrix: `identifier-matrix.md` — 528 files/8,317 `aisix` matches across six classification groups; providers/protocols/upstream all marked preserve.)_
- [x] 1.2 Verify the gateway’s upstream license, copyright, and redistribution/attribution evidence, including the missing gateway `LICENSE` file; resolve any incomplete evidence before distribution-facing branding changes. Verify that the recorded outcome names the source evidence and either permits rebranding with required notices or explicitly blocks distribution claims. _(Outcome: PERMITTED — upstream `github.com/api7/aisix` is public Apache-2.0 with a verbatim Apache-2.0 LICENSE and no NOTICE file; obligations: vendor LICENSE, add attribution notice, no AISIX/API7 product presentation. See matrix §3.)_
- [x] 1.3 Review the migration matrix and license/provenance outcome with the user and record approvals for final executable, package/crate, config/env prefix, container paths, header, and metric names, plus the control-plane surface decision. Verify that each externally consumed renamed identifier has an approved alias or migration instruction before implementation continues. _(Approved 2026-09-23: binary `sibyl-gateway`, full crate/type rename, env `SIBYL_GATEWAY_` with legacy alias, headers `x-sibylhub-*` with legacy request-id accept, metrics `sibyl_gateway_*`, etcd prefix `/sibyl-gateway`, control plane kept as labeled dormant compat, registry `ghcr.io/sibylhub/gateway`, glama removed, CLAUDE.md deferred. See matrix §5.)_

## 2. Monorepo and Turbo integration

- [x] 2.1 Add a thin `apps/gateway/package.json` bridge with the approved package name and deterministic `build`, `lint`, `typecheck`, `format`, `test`, `test:cov`, `clean`, and explicit `dev` scripts that invoke Cargo against the gateway’s independent workspace and committed lockfile; keep the e2e/MCP-conformance harness out of the root pnpm lockfile. Verify with `pnpm check:config`, `pnpm install --frozen-lockfile`, and `pnpm --filter gateway typecheck`. *(All three pass; harness stays in `apps/gateway/tests/e2e` with its own pnpm 11 environment, never in the root lockfile.)*
- [x] 2.2 Add a root `dev:gateway` command and ensure the ordinary root `dev` command does not silently start the gateway without required local configuration. Verify with `pnpm turbo run dev --filter=gateway --dry=json` that the gateway-only task graph exists and that `pnpm dev` remains parallel without requiring gateway config. _(Root `dev` now `--filter=!gateway`; `gateway#dev` task confirmed in dry-run, requires `config.local.yaml`.)_
- [x] 2.3 Update `turbo.json` and, if needed, package-level Turbo configuration so gateway tasks include the gateway source, configuration examples, `apps/gateway/Cargo.lock`, and `apps/gateway/rust-toolchain.toml` in task identity; declare the gateway build artifact explicitly and keep `dev`, deployment, database, remote-publish, and prerequisite-dependent e2e/conformance tasks uncached. Verify with `pnpm turbo run build --filter=gateway --dry=json` and by changing a gateway lockfile/toolchain input in a scratch check to confirm the task hash changes. _(Package `turbo.json` with explicit inputs — 298 crates/_* + 43 schemas/** + lockfile + toolchain hashed; outputs `target/release/sibyl-gateway`; `test:e2e`/`test:mcp-conformance` cache:false.)*
- [x] 2.4 Update `scripts/check-config.mjs`, root `README.md`, nested `apps/gateway/AGENTS.md` guidance, and `.gitignore`/local-config expectations so the gateway appears as a distinct app with Cargo ownership, commands, artifact, local-service prerequisites, and the local-versus-deployment evidence boundary. Verify that `pnpm check:config` passes and that every documented gateway command resolves to a real package or root task. _(check-config: 32 paths ok; `pnpm install --frozen-lockfile` ok; lockfile rewrite verified cosmetic — 1,919/1,919 integrity hashes identical + gateway importer.)_

## 3. SibylHub Gateway identity and compatibility

- [x] 3.1 Rebrand first-party gateway documentation and metadata to SibylHub Gateway, including README, roadmap, Docker/container labels, examples, help/version output, admin OpenAPI metadata, and supported workflows/tools, while preserving required upstream attribution and third-party provider/protocol names. Verify by inspecting the approved surfaces and running targeted searches showing no remaining current-product AISIX/API7 identity except preserved provenance, license, upstream references, or compatibility aliases. *(README/ROADMAP rewritten; Dockerfile label removed; entrypoint/workflows repointed to `ghcr.io/sibylhub/gateway`; OpenAPI title/description rebranded; clap about/help rebranded; `Server: SibylHub-Gateway/<ver>`; residual `aisix`/`api7` matches are preserved citations (`api7/AISIX-Cloud#N`, `api7/aisix#N`, `api7/ai-gateway#N`), NOTICE provenance, the `aisix-dp` UA, or the deferred `CLAUDE.md`.)*
- [x] 3.2 Rename approved gateway-owned runtime identifiers—executable, Cargo packages/crates as needed, config/env prefix, container paths, custom headers, and metrics—and implement aliases or explicit migration behavior for every externally consumed identifier. Verify with focused Rust/unit/e2e tests that both old compatibility values and new values behave as approved, and that generated schemas/OpenAPI are regenerated when affected. *(All renames landed per matrix §5; aliases: `AISIX_*` env with boot warning (`legacy_env_prefix_applies_only_when_new_prefix_is_absent`), clap config-path shim, `x-aisix-request-id` inbound default, `globalThis.aisix` script namespace. `cargo check/clippy/test --workspace --locked` green — 4,500 tests, 0 failures; schemas + OpenAPI regenerated.)*
- [x] 3.3 Resolve managed/control-plane branding and behavior according to the approved matrix: preserve the upstream wire contract as an explicitly labeled compatibility surface, disable it behind documented configuration, or defer it with user approval; do not advertise an unavailable SibylHub control plane. Verify the chosen behavior through configuration/startup tests and a documented operator-facing status note. *(Per matrix §5-G: wire contract untouched and default-off; `config.managed.yaml` header and README "Managed mode" section label it an upstream compatibility surface; managed-mode config parsing covered by the passing workspace tests; operator status note in README.)*
- [x] 3.4 Replace or explicitly mark unverified upstream brand assets, screenshots, architecture imagery, badges, external community/demo links, and container registry references. Verify that no first-party SibylHub surface presents upstream marketing assets as current product evidence and that placeholder or preserved-provenance status is explicit. *(Console screenshots, architecture SVG, `glama/`, `glama.json`, Discord/demo/api7.ai marketing links removed with the README rewrite; container registry references point at the planned `ghcr.io/sibylhub/gateway` with docs noting publication has not happened.)*

## 4. Documentation coordination

- [x] 4.1 Update the active `expand-docusaurus-documentation` proposal/design/tasks so the gateway is described as an integrated first-party SibylHub application rather than unavailable, and coordinate sidebar/page placement without duplicating gateway-owned runtime docs. Verify with `openspec validate expand-docusaurus-documentation` or the project’s equivalent strict validator and a readback that no stale “no first-party gateway” claim remains. _(Proposal/design/tasks updated; strict validation passes; gateway now distinguished from the still-unavailable Rosie/FastMCP execution source.)_
- [x] 4.2 Add or update `apps/docs` pages covering the gateway’s AI-traffic role, package/root commands, Cargo/lockfile/toolchain boundary, configuration prerequisites, local-service e2e boundary, and explicit deployment/runtime evidence limits. Verify with `pnpm --filter docs validate`, `pnpm --filter docs typecheck`, `pnpm --filter docs lint`, and `pnpm --filter docs build`. _(New `docs/gateway.md` + sidebar/intro/workspace/runtime-boundaries updates; all four gates pass, build emits `build/docs/gateway/`.)_

## 5. Validation and readback

- [x] 5.1 Run gateway package checks—`pnpm --filter gateway format`, `lint`, `typecheck`, `test`, and `test:cov`—and separately run any approved prerequisite-dependent e2e/MCP-conformance tasks only when their local services and Node/pnpm requirements are available. Record each result, including unavailable-prerequisite checks as not run rather than passed. *(fmt/clippy/typecheck/test/test:cov all pass locked: 4,500 tests, 0 failures. `test:e2e` and `test:mcp-conformance` NOT RUN — no local etcd/Redis, prerequisites unavailable; recorded as not run, not passed.)*
- [x] 5.2 Run root repository gates—`pnpm check:config`, `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm test:cov`, `pnpm format`, and `pnpm build`—and confirm the gateway participates without collapsing runtime boundaries. Record failures by owning package/task and distinguish local evidence from deployment, database, registry publication, remote control-plane, or provider-runtime evidence. *(check:config/lint/typecheck/test/test:cov/build all pass — `pnpm build` includes the gateway release binary. `pnpm format` fails on TWO PRE-EXISTING issues unmodified since the initial commit: `packages/schemas/fixtures/malformed-ecosystem.json` (intentionally malformed fixture Prettier cannot parse) and a Filament vendor JS asset — recorded as pre-existing, out of this change's scope.)*
- [x] 5.3 Validate the OpenSpec change strictly and inspect the final diff for secrets, fabricated production identifiers, unapproved brand claims, missing attribution, or accidental protocol/provider renames. Verify with `openspec validate integrate-sibylhub-gateway --strict` (or the project’s supported equivalent), `git diff --check`, and a targeted secret/branding review. *(Strict validation passes; branding sweep clean — residual `aisix`/`api7` matches are preserved citations, NOTICE provenance, the compat UA, legacy aliases, or deferred `CLAUDE.md`; test PEM fixtures are upstream-shipped test material; no credentials tracked.)*
