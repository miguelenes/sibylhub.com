## 1. Runtime and repository foundation

- [x] 1.1 Pin the supported Node, pnpm 9.15.9, PHP 8.3+, Composer, Rust, and Cargo toolchain expectations in repository version files and documentation, and verify them with the documented runtime version commands
- [x] 1.2 Create the root `package.json` with exact direct versions for Turborepo 2.10.13, Prettier 3.9.6, and TypeScript 5.9.3 plus root scripts for `dev`, `dev:web`, `dev:backend`, `build`, `test`, `lint`, `format`, `typecheck`, `test:cov`, `clean`, and non-mutating configuration checks; verify the manifest parses and every declared script resolves to a command
- [x] 1.3 Create `pnpm-workspace.yaml` for `apps/*` and `packages/*`, and verify pnpm discovers the expected workspace roots without traversing dependency or build directories
- [x] 1.4 Create the top-level Cargo workspace for `apps/backend` and `apps/cli` with resolver 2, shared package metadata, workspace dependency declarations, and a shared root target directory; verify `cargo metadata --no-deps` resolves both members
- [x] 1.5 Add root ignore rules and non-secret environment-shape files for Node, Astro/Cloudflare, Laravel, Cargo, Docusaurus, and `.agent/` state; verify no tracked file contains a credential, private key, fabricated production identifier, or real environment value
- [x] 1.6 Create `.agent/` declarative governance files plus root and nested `AGENTS.md` guidance for package ownership, safe commands, required gates, formatter ownership, deployment boundaries, and local-versus-remote evidence; verify each guidance file references existing paths and commands
- [x] 1.7 Add the root README with the final directory tree, prerequisites, setup sequence, development commands, testing gates, formatting ownership, and deployment exclusions; verify every documented path and root command matches the manifests

## 2. Shared configuration and schema contracts

- [x] 2.1 Create `packages/typescript-config` with a shared strict TypeScript base and package-specific extensions for Astro, Docusaurus, and schema validation; verify all TypeScript packages can extend it without duplicate incompatible compiler settings
- [x] 2.2 Create `packages/schemas` as a private versioned package with JSON Schema source files, deterministic build/export scripts, Zod validation entry points, and package metadata; verify package build and typecheck emit the declared artifacts
- [x] 2.3 Define the versioned ecosystem schema and explicit catalog of exactly 25 unique target language identities with linked runtime, package-manager, lockfile, builder, invariant, and documentation relationships; verify valid fixtures contain 25 entries and incomplete/unresolved fixtures fail validation
- [x] 2.4 Define schemas for `.agent/config.json`, `.agent/skills.json`, stack-invariant rules, schema-version envelopes, and PURLs with canonical component preservation; verify valid and malformed fixtures produce field-level validation results
- [x] 2.5 Implement the TypeScript/Zod validators and stable error/result shape for the shared schemas, rejecting unsupported schema versions, credentials, private keys, and executable directives; verify negative tests prove fail-closed behavior
- [x] 2.6 Add deterministic cross-runtime fixtures for valid, incomplete, incompatible, secret-bearing, and malformed documents, and document how PHP and Rust consumers load them; verify repeated generation produces byte-identical fixture files
- [x] 2.7 Add schema package lint, format, test, typecheck, coverage, and clean scripts; verify each script runs locally without a backend, database, Cloudflare resource, or production credential

## 3. Turborepo orchestration and root gates

- [x] 3.1 Create `turbo.json` using the current `tasks` schema with `build` topological dependencies, declared web/docs/backoffice/Cargo outputs, relevant lockfile/config inputs, and non-cached persistent `dev` tasks; verify `pnpm turbo run build --dry` reports the intended graph
- [x] 3.2 Add uncached task definitions for deployment, remote registry publication, database migrations, and other side-effecting operations with no dependency from persistent development tasks; verify a task configuration inspection shows no side-effecting task can be restored from cache
- [x] 3.3 Implement the root formatting dispatcher so Prettier handles TypeScript, TSX, Astro, JSON, and Markdown while Pint handles PHP and Cargo handles Rust; verify each formatter’s check mode passes on its owned file types and no broad invalid Prettier glob remains
- [x] 3.4 Implement the root non-mutating configuration check for workspace discovery, required manifests, schema artifacts, runtime version constraints, and Cloudflare binding shape; verify it fails before any network call when a required local configuration contract is absent
- [x] 3.5 Add a CI-ready workflow or equivalent documented automation that installs from committed lockfiles and runs graph, lint, typecheck, test, coverage, format, and build gates; verify its commands match the root scripts and expose package-level failures

## 4. Astro, React, and Cloudflare web application

- [x] 4.1 Scaffold `apps/web` with Astro 5.18.2, React and React DOM 19.2.0, `@astrojs/react` 4.3.1, and `@astrojs/cloudflare` 12.6.13 using the shared TypeScript configuration; verify the generated application passes its initial Astro type/configuration check
- [x] 4.2 Configure Astro for the selected hybrid/SSR Cloudflare Workers output and add an explicit Worker entry/build configuration; verify a local Worker build produces the expected server artifact without requiring a live Cloudflare account
- [x] 4.3 Add Wrangler 4.59.2 configuration for the named `DB`, `AST_STORAGE`, and `VECTORIZE_INDEX` bindings, with local/preview and remote environments separated and no fake identifiers; verify the local binding-shape check passes and missing remote targets fail closed before contact with Cloudflare
- [x] 4.4 Integrate Tailwind CSS 4.3.3 through `@tailwindcss/vite` and CSS-first imports, and configure HeroUI React/Styles 3.2.5 without a provider or v2 API; verify dependency versions, CSS generation, and source imports match the selected v4/v3 contracts
- [x] 4.5 Implement the baseline public route with server-rendered Astro content and a narrowly scoped React 19 island using accessible HeroUI v3 semantics and event handling; verify the rendered HTML contains non-interactive content without unnecessary hydration and the island accessibility test passes
- [x] 4.6 Add typed request-time interfaces for D1 reads, R2 AST storage, and Vectorize access with deterministic local doubles; verify the baseline route and unit tests run with the bindings absent and the doubles exercise each interface without remote calls
- [x] 4.7 Add web scripts for development, build, Worker preview, explicit `wrangler deploy`, configuration validation, lint, typecheck, test, coverage, and clean; verify preview uses the Worker path and no `wrangler pages deploy ./dist` command is used for the SSR application
- [x] 4.8 Add focused web tests for route rendering, React island interaction, binding absence, configuration failure, and secret-safe diagnostics; verify the focused suite passes without a backend or production Cloudflare resource
- [x] 4.9 Run the web production build and inspect its output, hydration boundaries, Worker artifact, and binding declarations; verify the baseline route is reachable in local preview and no unresolved imports or unintended client-wide hydration remain

## 5. Laravel, Filament, and ecosystem source of truth

- [x] 5.1 Create `apps/backoffice` through the Laravel installer using PHP 8.3+ and Laravel 12.69.2, retain its Composer and frontend manifests inside the app, and add the Turborepo bridge manifest; verify `php artisan about` and the local boot command succeed with non-secret configuration
- [x] 5.2 Install Filament 5.8.1 and Laravel Boost 2.9.0 through their supported workflows, configure the panel boundary, and add local authentication/authorization-safe defaults; verify `php artisan route:list` exposes the panel route and a request returns the panel shell or an explicit auth response
- [x] 5.3 Configure local and test environments with SQLite or another documented ephemeral database, safe cache/session/queue drivers, and no production service dependency; verify application tests boot from a fresh local test database without external credentials
- [x] 5.4 Implement revision-aware normalized registry storage for the 25 languages, runtimes, package managers, lockfile specifications, builders, stack invariants, documentation references, and compatibility relationships; verify migrations, unique stable identities, relationship constraints, and rollback behavior with database tests
- [x] 5.5 Implement typed validation and authorization boundaries that validate registry revisions against `packages/schemas`, reject unresolved relationships, and prevent incomplete revisions from becoming exportable; verify valid, incomplete, incompatible, and secret-bearing fixtures produce the expected rejection/acceptance results
- [x] 5.6 Create the initial Filament registry resources and forms for editing the governed source data without product-specific administration; verify panel navigation, validation messages, relationship selection, and revision status are reachable through local requests
- [x] 5.7 Implement `php artisan registry:export-public` to select a valid revision, sort by stable identity, serialize schema-versioned deterministic JSON, and write a local R2-compatible export tree; verify repeated exports are byte-identical and every artifact validates against the shared schemas
- [x] 5.8 Add a separately explicit public-publication operation for reviewed exports that requires configured target and authorization, while keeping ordinary export/tests side-effect-free; verify missing target/authorization fails before any network call and local export never mutates remote storage
- [x] 5.9 Add backoffice scripts for development, Vite asset build, Pint check, route/config inspection, application tests, coverage, and clean; verify native Composer, Artisan, and Vite failures preserve their exit codes through the adapter
- [x] 5.10 Add focused Laravel/Pest or PHPUnit tests for boot, panel routing, registry relationships, validation, deterministic export, and secret scanning; verify the suite passes against local test state and reports optional coverage tooling separately when unavailable

## 6. Rust backend and workstation CLI

- [x] 6.1 Create independent `apps/backend` and `apps/cli` Cargo packages with exact compatible direct dependency versions for the selected Axum 0.7, Tokio 1.40, Serde 1.x, SQLx 0.8, Tower HTTP 0.6, Clap 4.x, Reqwest 0.12, Indicatif 0.17, tracing, `thiserror`, and `anyhow` lines; verify `cargo metadata --locked` resolves both binaries from the committed root lockfile
- [x] 6.2 Implement shared Rust configuration parsing, typed errors, structured tracing, and secret-redacting diagnostics, using `thiserror` for library/request errors and `anyhow` only at binary orchestration boundaries; verify invalid configuration exits non-zero without panic or secret leakage
- [x] 6.3 Implement the backend server with `GET /healthz`, stable readiness JSON, graceful startup/shutdown, and documented local Cargo/Turborepo commands; verify an in-process or local request returns the documented successful response
- [x] 6.4 Implement schema-validated registry snapshot loading for `GET /v1/ecosystems`, with local fixture support and backend-owned SQLx operational state kept separate from Laravel tables; verify a valid snapshot returns a schema-identifiable response and an invalid snapshot fails without being served
- [x] 6.5 Implement `POST /v1/invariants/validate` or the documented equivalent with structured client/internal error responses and invariant evaluation against shared fixtures; verify malformed input returns a client error without terminating the server and internal failures omit secret material
- [x] 6.6 Implement the independent CLI command tree with `sibyl --help`, version output, configuration diagnostics, non-zero invalid invocation behavior, and no bootstrap network/state mutation; verify help/version succeed and unknown commands fail safely
- [x] 6.7 Implement `sibyl init` as a read-only manifest analyzer that writes schema-valid `.agent/` metadata only after conflict checks and explicit overwrite handling; verify supported manifests produce deterministic metadata and incompatible existing configuration is never silently overwritten
- [x] 6.8 Implement `sibyl check` as a read-only invariant/package validation command with stable machine-readable and human-readable diagnostics; verify compliant projects exit zero, violations exit non-zero, and the project tree is unchanged
- [x] 6.9 Implement `sibyl sync` with schema validation, explicit endpoint/authorization configuration, Reqwest transport, bounded retry/error behavior, and acknowledgement/revision reporting for episodic-memory and AST-skeleton payloads; verify absent prerequisites and timeouts fail without claiming convergence or logging credentials
- [x] 6.10 Add Rust unit/integration fixtures for health, API errors, registry snapshots, invariant violations, CLI commands, initialization conflicts, and sync failure modes; verify baseline tests run without provider credentials or unintended production network access
- [x] 6.11 Add Rust adapter scripts for development, release build, format, Clippy, tests, supported coverage, and clean, including the documented `cargo watch` prerequisite if used; verify `cargo fmt --check`, locked Clippy with warnings denied, locked tests, and locked release builds pass offline or identify an explicit external prerequisite

## 7. Docusaurus documentation site

- [x] 7.1 Scaffold `apps/docs` with Docusaurus core/preset classic 3.10.2, `@mdx-js/react` 3.1.1, React 19, and the shared TypeScript configuration; verify the generated TypeScript configuration and local introduction route work
- [x] 7.2 Write documentation for the final workspace tree, package ownership, setup/prerequisites, root and package commands, Turborepo caching, `.agent/` governance, shared schemas, AST/Socraticode compression concepts, Context7 architecture, and deployment/security boundaries; verify each command/path matches the implementation manifests
- [x] 7.3 Add docs scripts for development on port 3001, static build, typecheck, lint/format, link/config validation, test/validation, and clean; verify each runs without a backend, database, Cloudflare resource, credential, or synchronization service
- [x] 7.4 Configure broken-link and invalid-configuration failures and build the static `build/` artifact; verify the output is non-empty, contains the introduction and referenced documents, and fails on an intentionally invalid internal link during the focused validation

## 8. Cross-runtime wiring and finish gates

- [x] 8.1 Add package-level Turborepo adapter scripts for backoffice, backend, CLI, docs, web, and schemas while preserving each native tool’s lifecycle and exit status; verify workspace package discovery lists every expected task implementation
- [x] 8.2 Connect declared build outputs and inputs for web, docs, backoffice assets, schemas, and root Cargo release binaries, and verify `pnpm turbo run build --dry` shows dependency order without output collisions
- [x] 8.3 Verify `pnpm turbo run dev --filter=web --filter=backend` starts both development processes concurrently with distinct ports and remains uncached; stop the processes and verify no deployment or publication command was invoked
- [x] 8.4 Validate a clean-checkout setup from committed pnpm, Composer, and Cargo lockfiles using only documented local environment examples; verify all applications/packages are discovered and setup does not require production credentials
- [x] 8.5 Run focused package checks for schemas, web, backoffice, backend, CLI, and docs, recording each result independently; verify every capability scenario has a passing check or a clearly reported blocked external prerequisite
- [x] 8.6 Run the full root `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm test:cov`, `pnpm format` check, and `pnpm build` gates; verify each result is reported separately and all declared build outputs exist
- [x] 8.7 Repeat a reproducible build and then change one declared source/configuration input; verify the unchanged build can hit cache, the changed task invalidates, and dev/deploy/publication/database tasks never restore cached completion
- [x] 8.8 Scan tracked files, generated manifests, lockfiles, environment examples, Wrangler configuration, logs, and documentation for credentials, private keys, fake resource IDs, accidental provider calls, and executable governance content; verify the scan is clean and `.agent/` remains declarative
- [x] 8.9 Review the final directory tree and README against the proposal/specs/design, verify no undocumented package or cross-runtime dependency was introduced, and record that no Cloudflare resource, DNS record, production database, secret, deployment, or remote sync was mutated
