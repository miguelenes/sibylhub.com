{
  "$schema": "https://turborepo.com/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", "*.config.*", "*.toml", "*.json"]
    },
    "dev": { "cache": false, "persistent": true },
    "deploy": { "cache": false },
    "registry:publish": { "cache": false },
    "migrate": { "cache": false },
    "lint": { "dependsOn": ["^build", "^lint"] },
    "typecheck": { "dependsOn": ["^build", "^typecheck"] },
    "test": { "dependsOn": ["^build", "^test"] },
    "test:cov": { "dependsOn": ["^build", "^test:cov"], "cache": false },
    "format": { "cache": false },
    "clean": { "cache": false }
  }
}
