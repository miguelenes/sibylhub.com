---
sidebar_position: 4
---

# Agent governance

`.agent/` contains declarative metadata. `AGENTS.md` files explain ownership, local commands, formatter responsibility, required gates, and the difference between local and remote evidence. The validator rejects credentials, private keys, and executable directives. `sibyl init` reads manifests without executing project code.
