# Documentation guidance

Docusaurus owns the static documentation build and uses port 3001 for local development. Keep internal links valid and use `onBrokenLinks: "throw"` and `onBrokenMarkdownLinks: "throw"`. Documentation may describe remote operations, but its local commands must not call them.
