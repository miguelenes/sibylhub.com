[package]
name = "sibyl-gateway-provider-bedrock"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "sibyl-gateway: AWS Bedrock runtime provider bridge (skeleton — multi-publisher dispatch)"

[dependencies]
tracing.workspace = true
sibyl-gateway-core = { path = "../sibyl-gateway-core" }
sibyl-gateway-hub = { path = "../sibyl-gateway-hub", features = ["aws"] }
# Reuse Anthropic Messages request/response wire types for the
# `anthropic.*` Bedrock publisher (Claude on Bedrock) non-stream
# /invoke path. The body shape is the Anthropic Messages API minus
# the API-key header plus `anthropic_version: "bedrock-2023-05-31"`.
# Other publishers (Meta / Mistral / Cohere / Amazon Titan/Nova /
# AI21) and all streaming go through the unified Converse API
# instead — see `chat_converse` / `chat_converse_stream`.
sibyl-gateway-provider-anthropic = { path = "../sibyl-gateway-provider-anthropic" }
# Reuse the per-ProviderKey request-override primitives (param_renames /
# param_constraints / default_body_fields / content_list_to_string) so every
# adapter applies the same wire transforms cp-api configures (#302 §5 / #340).
sibyl-gateway-provider-openai = { path = "../sibyl-gateway-provider-openai" }
async-stream = "0.3"
async-trait.workspace = true
serde.workspace = true
serde_json.workspace = true
tokio.workspace = true
http.workspace = true
# AWS SDK: handles SigV4 + retries + binary event-stream framing.
# Same workspace deps the guardrails crate uses for ApplyGuardrail.
aws-config.workspace = true
aws-sdk-bedrockruntime.workspace = true
aws-credential-types.workspace = true
aws-smithy-async.workspace = true
aws-smithy-runtime-api.workspace = true
aws-smithy-types.workspace = true

[dev-dependencies]
futures.workspace = true
tokio = { workspace = true, features = ["macros", "rt", "time"] }
wiremock.workspace = true
http.workspace = true
