# Template for running the `objstore_smoke_*` round-trips against REAL cloud
# storage (the credential_ref / static-key path). Copy to a gitignored file,
# fill in real values, then:
#   set -a; source crates/sibyl-gateway-obs/tests/real-cloud.env; set +a
#   cargo test -p sibyl-gateway-obs -- --ignored --nocapture objstore_smoke_s3 objstore_smoke_gcs objstore_smoke_azure
#
# In CI these same names are repository secrets — see
# .github/workflows/objstore-real-cloud.yml. DO NOT commit a filled-in copy;
# these are real credentials (`real-cloud.env` is gitignored).

# ── Amazon S3 (native AWS) ──
# Least privilege — scope an IAM user to the test bucket only:
#   s3:PutObject, s3:GetObject, s3:DeleteObject  on  arn:aws:s3:::<bucket>/*
#   s3:ListBucket                                on  arn:aws:s3:::<bucket>
SIBYL_GATEWAY_E2E_OBJSTORE_S3_BUCKET=your-sibyl-gateway-smoke-bucket
SIBYL_GATEWAY_E2E_OBJSTORE_S3_REGION=us-east-1
SIBYL_GATEWAY_E2E_OBJSTORE_S3_ACCESS_KEY_ID=AKIAxxxxxxxxxxxxxxxx
SIBYL_GATEWAY_E2E_OBJSTORE_S3_SECRET_ACCESS_KEY=your-secret-access-key
# For an S3-compatible host (MinIO / Cloudflare R2 / OSS) instead of native AWS,
# also set the endpoint (path-style):
# SIBYL_GATEWAY_E2E_OBJSTORE_S3_ENDPOINT=https://your-s3-compatible-host

# ── Google Cloud Storage ──
# Grant the service account roles/storage.objectAdmin on the test bucket. Paste
# the full downloaded key JSON, single-quoted (it contains commas):
SIBYL_GATEWAY_E2E_OBJSTORE_GCS_BUCKET=your-sibyl-gateway-smoke-bucket
SIBYL_GATEWAY_E2E_OBJSTORE_GCS_SERVICE_ACCOUNT='{"type":"service_account","project_id":"your-project","private_key_id":"...","private_key":"-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n","client_email":"sibyl-gateway-smoke@your-project.iam.gserviceaccount.com","client_id":"...","token_uri":"https://oauth2.googleapis.com/token"}'

# ── Azure Blob ──
# An account key is account-wide, so use a DEDICATED test storage account.
# (For the Azurite emulator instead, also set SIBYL_GATEWAY_E2E_OBJSTORE_AZURE_ENDPOINT
#  to http://127.0.0.1:10000/devstoreaccount1.)
SIBYL_GATEWAY_E2E_OBJSTORE_AZURE_ACCOUNT=youraisixsmokeacct
SIBYL_GATEWAY_E2E_OBJSTORE_AZURE_ACCESS_KEY=your-account-access-key
SIBYL_GATEWAY_E2E_OBJSTORE_AZURE_CONTAINER=sibyl-gateway-smoke
